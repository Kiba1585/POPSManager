<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========================================================= -->
    <!--  COLORES POR TIPO DE NOTIFICACIÓN                        -->
    <!-- ========================================================= -->

    <SolidColorBrush x:Key="ToastSuccess" Color="#4CAF50"/>
    <SolidColorBrush x:Key="ToastInfo" Color="#2196F3"/>
    <SolidColorBrush x:Key="ToastWarning" Color="#FFC107"/>
    <SolidColorBrush x:Key="ToastError" Color="#F44336"/>


    <!-- ========================================================= -->
    <!--  ESTILO BASE DEL TOAST                                    -->
    <!-- ========================================================= -->

    <Style x:Key="ToastBase" TargetType="Border">
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="12 10"/>
        <Setter Property="Margin" Value="0 0 0 10"/>
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="HorizontalAlignment" Value="Center">
        </Setter>
        <Setter Property="Width" Value="380"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black"
                                  BlurRadius="18"
                                  ShadowDepth="2"
                                  Opacity="0.35"/>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ========================================================= -->
    <!--  TOAST SUCCESS                                             -->
    <!-- ========================================================= -->

    <Style x:Key="ToastSuccessStyle" TargetType="Border" BasedOn="{StaticResource ToastBase}">
        <Setter Property="Background" Value="{StaticResource ToastSuccess}"/>
    </Style>

    <!-- ========================================================= -->
    <!--  TOAST INFO                                                -->
    <!-- ========================================================= -->

    <Style x:Key="ToastInfoStyle" TargetType="Border" BasedOn="{StaticResource ToastBase}">
        <Setter Property="Background" Value="{StaticResource ToastInfo}"/>
    </Style>

    <!-- ========================================================= -->
    <!--  TOAST WARNING                                             -->
    <!-- ========================================================= -->

    <Style x:Key="ToastWarningStyle" TargetType="Border" BasedOn="{StaticResource ToastBase}">
        <Setter Property="Background" Value="{StaticResource ToastWarning}"/>
    </Style>

    <!-- ========================================================= -->
    <!--  TOAST ERROR                                               -->
    <!-- ========================================================= -->

    <Style x:Key="ToastErrorStyle" TargetType="Border" BasedOn="{StaticResource ToastBase}">
        <Setter Property="Background" Value="{StaticResource ToastError}"/>
    </Style>


    <!-- ========================================================= -->
    <!--  ANIMACIONES DE ENTRADA Y SALIDA                          -->
    <!-- ========================================================= -->

    <Storyboard x:Key="ToastShowAnimation">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1"
                         Duration="0:0:0.25"
                         FillBehavior="HoldEnd"/>

        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                         From="-20" To="0"
                         Duration="0:0:0.25"
                         FillBehavior="HoldEnd"/>
    </Storyboard>

    <Storyboard x:Key="ToastHideAnimation">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="1" To="0"
                         Duration="0:0:0.40"
                         FillBehavior="Stop"/>

        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                         From="0" To="-20"
                         Duration="0:0:0.40"
                         FillBehavior="Stop"/>
    </Storyboard>

</ResourceDictionary>
