<UserControl x:Class="POPSManager.Views.ConvertView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:POPSManager.ViewModels"
             Background="{StaticResource Brush.Background}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Convertir BIN / CUE / ISO a VCD"
                   FontSize="28"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource Brush.TextPrimary}"
                   Margin="0 0 0 20"/>

        <Border Grid.Row="1"
                Style="{StaticResource Card}"
                Padding="20"
                Effect="{StaticResource ShadowDefault}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Carpeta origen -->
                <StackPanel Grid.Row="0" Grid.Column="0">
                    <TextBlock Text="Carpeta de origen"
                               FontSize="16"
                               Foreground="{StaticResource Brush.TextSecondary}"
                               Margin="0 0 0 6"/>
                    <TextBox Text="{Binding SourcePath, UpdateSourceTrigger=PropertyChanged}"
                             Height="32"
                             Background="{StaticResource Brush.PanelAlt}"
                             Foreground="{StaticResource Brush.TextPrimary}"
                             BorderBrush="{StaticResource Brush.Border}"
                             BorderThickness="1"
                             Padding="6"/>
                </StackPanel>

                <Button Grid.Row="0" Grid.Column="1"
                        Content="Examinar"
                        Width="120"
                        Height="32"
                        Margin="10 0 0 0"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding BrowseSourceCommand}"/>

                <!-- Carpeta destino -->
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0 15 0 0">
                    <TextBlock Text="Carpeta de destino"
                               FontSize="16"
                               Foreground="{StaticResource Brush.TextSecondary}"
                               Margin="0 0 0 6"/>
                    <TextBox Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}"
                             Height="32"
                             Background="{StaticResource Brush.PanelAlt}"
                             Foreground="{StaticResource Brush.TextPrimary}"
                             BorderBrush="{StaticResource Brush.Border}"
                             BorderThickness="1"
                             Padding="6"/>
                </StackPanel>

                <Button Grid.Row="1" Grid.Column="1"
                        Content="Examinar"
                        Width="120"
                        Height="32"
                        Margin="10 15 0 0"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding BrowseOutputCommand}"/>

                <!-- Lista de archivos -->
                <Border Grid.Row="2" Grid.ColumnSpan="2"
                        Style="{StaticResource Card}"
                        Margin="0 20 0 0"
                        Padding="10"
                        Background="{StaticResource Brush.PanelAlt}"
                        Effect="{StaticResource ShadowSoft}">
                    <StackPanel>
                        <TextBlock Text="Archivos detectados"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource Brush.TextPrimary}"
                                   Margin="0 0 0 10"/>
                        <ListBox ItemsSource="{Binding Files}"
                                 Height="260"
                                 Background="{StaticResource Brush.Panel}"
                                 Foreground="{StaticResource Brush.TextPrimary}"
                                 BorderThickness="0"
                                 Padding="6"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Button Grid.Row="2"
                Content="Convertir archivos"
                Height="42"
                Width="220"
                HorizontalAlignment="Right"
                Margin="0 20 0 0"
                Style="{StaticResource PrimaryButton}"
                Command="{Binding ConvertCommand}"/>
    </Grid>
</UserControl>